const Vaults = () => {
  // Empty dataset - no vaults to display
  const vaultsData = [];

  return (
    <div className="bg-[#1e2d29] rounded-xl shadow-lg overflow-hidden"></div>
  );
};

export default Vaults;
