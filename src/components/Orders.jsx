const Orders = () => {
  // Empty dataset - no orders to display
  const ordersData = [];

  return (
    <div className="bg-[#1e2d29] rounded-xl shadow-lg overflow-hidden"></div>
  );
};

export default Orders;
